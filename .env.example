# Reserved for a future OpenAI-compatible provider. NOT functional today:
# the app runs fully offline on the built-in scripted tutor, and wiring a real
# provider would need a server-side route so the API key stays out of the client.
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
